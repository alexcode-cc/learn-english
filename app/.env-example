# ============================================
# Vite 開發伺服器設定
# ============================================

# 開發伺服器的主機名稱
# 預設值: localhost
VITE_HOST=localhost

# 開發伺服器端口號
# 預設值: 5173
VITE_PORT=5173

# 域名設定（用於 SSL 證書路徑）
# 此設定用於構建 SSL 證書路徑，應與 ssl/ 目錄下的證書目錄名稱一致
# 預設值: localhost
VITE_DOMAIN=learn.english.org

# 是否啟用 SSL/HTTPS
# 設為 'true' 時會嘗試載入 SSL 證書，需要確保 ssl/{VITE_DOMAIN}/ 目錄下有對應的證書檔案
# 預設值: false
VITE_SSL_ENABLED=false

# E2E 測試模式
# 設為 'true' 時會禁用 HTTPS（即使 VITE_SSL_ENABLED 為 true）
# 預設值: false
VITE_E2E_TEST=false

# ============================================
# 應用程式設定
# ============================================

# 字典 API 基礎 URL
# 預設值: https://api.dictionaryapi.dev/api/v2/entries/en
VITE_DICTIONARY_BASE_URL=https://api.dictionaryapi.dev/api/v2/entries/en

# ============================================
# 測試設定（可選）
# ============================================

# Playwright 測試基礎 URL
# 預設值: http://localhost:5173
# PLAYWRIGHT_TEST_BASE_URL=http://localhost:5173